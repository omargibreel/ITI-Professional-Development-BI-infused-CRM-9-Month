// Step 1: Install Node.js (if not already installed)
console.log("1. Download and install Node.js from https://nodejs.org");

// Step 2: Install TypeScript globally using npm
console.log("2. Run: npm install -g typescript");

// Step 3: Verify installation
console.log("3. Run: tsc --version");

// Step 4: Create a tsconfig.json file
console.log("4. Run: tsc --init");

// Step 5: Write your TypeScript code in .ts files
console.log("5. Create files with .ts extension and write TypeScript code");

// Step 6: Compile TypeScript to JavaScript
console.log("6. Run: tsc to compile all files or tsc filename.ts for a specific file");
console.log("6. Run: tsc file.ts --watch"); //specific file


// error lens --> extenstion